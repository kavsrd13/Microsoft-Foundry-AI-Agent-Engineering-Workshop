# Lab 01: Foundry setup

This lab checks the shared project endpoint and Entra credential used by the workshop.

Use the shared `.env` file at the workshop root, install the lab dependencies when present, and run:

```powershell
python main.py
```

The code uses `DefaultAzureCredential`, so sign in with Azure CLI before running it.
