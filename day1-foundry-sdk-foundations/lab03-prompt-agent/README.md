# Lab 03: Prompt agent

This lab creates a minimal prompt agent with the Foundry SDK.

Use the shared `.env` file at the workshop root and run:

```powershell
python main.py
```

The example keeps the agent definition and model call visible in one small file.
